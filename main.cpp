#include <iostream>
#include "AnimationWindow.h"
#include "include/window.h"

int main() {
    bool run = 1; 

    while (run) {
        std::cout << "Game is running" << std::endl;
        run = 0; 

    }
    return 0;
}